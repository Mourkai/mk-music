<view class="video">
    <view class="item" wx:for="{{topMvs}}" wx:key="id">
        <video-item-v1 item="{{item}}" data-item="{{item}}" bindtap="handleVideoClick"></video-item-v1>   
    </view>
</view>