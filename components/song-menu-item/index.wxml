<!--components/song-menu-item/index.wxml-->
<wxs src="../../utils/format.wxs" module="format"></wxs>
<view class="item">
    <view class="top">
        <image class="image" src="{{data.coverImgUrl}}"></image>
        <view class="play-counter">{{format.formatCount(data.playCount)}}</view>
    </view>
    <view class="bottom">
        {{data.name}}
    </view>
</view>